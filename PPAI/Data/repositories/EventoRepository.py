# Este módulo fue eliminado por diseño: usar mappers + Data.dao.baseDao / SessionLocal directamente.
raise ImportError("EventoRepository eliminado. Usa Data.mappers y Data.dao.baseDao / SessionLocal para persistir/consultar.")
